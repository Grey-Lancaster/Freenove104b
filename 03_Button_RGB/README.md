# Chapter 3.1 — Button + RGB LED

Ported from Freenove's `Sketch_03.1_Button_RGB.ino`. Each press of the
onboard BOOT button (debounced by polling) advances the WS2812 LED
through red, green, blue, and off, in order.
