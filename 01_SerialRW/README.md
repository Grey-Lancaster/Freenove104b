# Chapter 1 — Serial Read/Write

Ported from Freenove's `Sketch_01.1_SerialRW.ino`. The simplest possible
USB-serial round trip: reads characters over `Serial` until it sees a
newline, then echoes the accumulated line straight back.
