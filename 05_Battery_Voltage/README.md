# Chapter 5.1 — Battery Voltage

Ported from Freenove's `Sketch_05.1_Battery_Voltage.ino`. Reads the
battery-monitor ADC pin every 300ms and prints both the raw ADC value
and the calculated battery voltage (the pin's millivolt reading, doubled
for the onboard voltage divider) over serial.
